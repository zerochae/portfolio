import styled from "styled-components";

const Container = styled.div`
  height: calc(100vh - 70px);

`;
export default function Home() {
  return <Container className="nav">home
  &lt;portfolio&gt;
  &lt;/portfolio&gt;
  </Container>;
}
